{
  "name": "eslint-plugin-relay",
  "version": "0.0.1",
  "description": "ESLint plugin for Relay.",
  "main": "eslint-plugin-relay",
  "repository": "relayjs/eslint-plugin-relay",
  "license": "BSD-3-Clause",
  "scripts": {
    "test": "mocha",
    "test-watch": "mocha --watch",
    "prettier": "find . -name node_modules -prune -or -name '*.js' -exec prettier --write --single-quote --trailing-comma es5 --no-bracket-spacing --jsx-bracket-same-line --parser flow {} +"
  },
  "files": [
    "eslint-plugin-relay.js",
    "LICENSE",
    "PATENTS"
  ],
  "dependencies": {
    "eslint": "3",
    "graphql": "^0.10.1",
    "mocha": "^3.4.2",
    "prettier": "^1.4.1"
  }
}
