{
  "name": "eslint-plugin-relay",
  "version": "0.0.8",
  "description": "ESLint plugin for Relay.",
  "main": "eslint-plugin-relay",
  "repository": "relayjs/eslint-plugin-relay",
  "license": "BSD-3-Clause",
  "scripts": {
    "test": "mocha",
    "test-watch": "mocha --watch",
    "prettier": "find . -name node_modules -prune -or -name '*.js' -exec prettier --write --single-quote --trailing-comma none --no-bracket-spacing --jsx-bracket-same-line --parser flow {} +"
  },
  "files": [
    "eslint-plugin-relay.js",
    "LICENSE",
    "PATENTS"
  ],
  "dependencies": {
    "graphql": "^0.10.1"
  },
  "devDependencies": {
    "babel-eslint": "^7.2.3",
    "eslint": "3.19.0",
    "mocha": "^3.4.2",
    "prettier": "^1.4.4"
  }
}
